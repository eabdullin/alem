"use client";

import type { ToolDisplayProps } from "../types";

/**
 * Memory tool display: compact output for view/search results.
 * Memory writes are kept invisible per instructions; display shows read/search output when present.
 */
export function MemoryToolDisplay({
  output,
  errorText,
}: ToolDisplayProps) {

  return null;
}
